# CGO -buildmode=c-shared outputs
/cursor.h
/cursor.so

# Local packaging output. Publish these from GitHub Releases, not git.
/release/*.tar.gz
/release/*.zip
/release/checksums.txt

# Go test and coverage leftovers
*.test
*.out
coverage.html
